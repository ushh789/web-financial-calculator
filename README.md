# web-financial-calculator

Monorepo layout:
- `api` backend (Spring Boot)
- `ui` frontend (Vite)

Quick start:
- Backend: `cd api` then `mvn clean install`
- Frontend: `cd ui` then `npm install` and `npm run dev`
